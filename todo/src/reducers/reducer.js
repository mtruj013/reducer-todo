import React, { useState, useReducer } from 'react';

const initialState = {
    item: 'learn abnout reducers',
    completed: false,
    id: 3892987589
}

const reducer = (state, action) => {
    switch(action.type){
        default:
            return state;
    }
}
